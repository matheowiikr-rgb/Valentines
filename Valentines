index.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For Helene ❤️</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(180deg, #ff4d6d, #ff8fab);
      color: white;
      text-align: center;
      min-height: 100vh;
      overflow-x: hidden;
    }

    h1 {
      margin-top: 60px;
      font-size: 2.5rem;
    }

    h2 {
      font-weight: 300;
      margin-bottom: 30px;
    }

    .card {
      background: rgba(255, 255, 255, 0.18);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 25px;
      max-width: 90%;
      margin: auto;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
    }

    button {
      background: white;
      color: #ff4d6d;
      border: none;
      padding: 15px 25px;
      font-size: 1rem;
      border-radius: 30px;
      cursor: pointer;
      margin-top: 20px;
      transition: transform 0.2s ease;
    }

    button:hover {
      transform: scale(1.05);
    }

    #surprise {
      display: none;
      margin-top: 20px;
      font-size: 1.05rem;
      line-height: 1.5;
    }

    .petal {
      position: fixed;
      top: -40px;
      font-size: 1.8rem;
      animation: fall linear forwards;
      pointer-events: none;
      opacity: 0.9;
    }

    @keyframes fall {
      0% {
        transform: translateY(0) rotate(0deg);
      }
      100% {
        transform: translateY(110vh) rotate(360deg);
      }
    }

    .flowers {
      margin-top: 40px;
      font-size: 2rem;
    }
  </style>
</head>
<body>

  <h1>Helene ❤️</h1>
  <h2>Vil du tilbringe valentinsdagen med meg?❤️</h2>

    <button onclick="openSurprise()">Open 💝</button>

    <div id="surprise">
      <p>
        Hello my darling❤️  
        Eg gleder meg ekstremt til denne dagen med deg❤️  
        Eg elsker deg så uendelig mye my sweetheart❤️❤️❤️❤️❤️
      </p>
    </div>
  </div>

  <div class="flowers">🌹 🌸 🌷 🌺 🌹 🌸 🌷 🌺</div>

  <script>
    function openSurprise() {
      document.getElementById("surprise").style.display = "block";
    }

    // Falling flower petals
    const petals = ["🌸", "🌹", "🌺", "🌷"];

    function createPetal() {
      const petal = document.createElement("div");
      petal.className = "petal";
      petal.innerText = petals[Math.floor(Math.random() * petals.length)];
      petal.style.left = Math.random() * 100 + "vw";
      petal.style.animationDuration = (4 + Math.random() * 4) + "s";
      document.body.appendChild(petal);

      setTimeout(() => {
        petal.remove();
      }, 8000);
    }

    setInterval(createPetal, 350);
  </script>

</body>
</html>
